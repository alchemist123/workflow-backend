# ── Web framework ─────────────────────────────────────────────────────────────
# Floors are set by google-adk 2.8: fastapi>=0.133, starlette>=1.3.1, pydantic>=2.12.
fastapi>=0.133,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.12,<3
pydantic-settings>=2.2.1
python-multipart>=0.0.9

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# ── Compiler / canvas validation ──────────────────────────────────────────────
jsonschema>=4.23,<5
networkx==3.3
jmespath==1.0.1
jinja2>=3.1.4,<4

# ── Google ADK — graph workflows + A2A server ─────────────────────────────────
# a2a-sdk is pinned below 1.0 deliberately: in 1.x AgentCard became a protobuf
# message that drops `url`, `preferred_transport` and `protocol_version`, which
# the generated agent cards rely on. ADK supports both generations, so an
# unpinned install would silently resolve to 1.x and break card construction.
google-adk>=2.8,<3
a2a-sdk[http-server]>=0.3.4,<1.0
# mcp 2.x renamed the streamable-HTTP client from `streamablehttp_client` to
# `streamable_http_client`; app/nodes and the package template use the 1.x name.
# google-adk does not constrain mcp, so pin it here.
mcp>=1.9,<2

# ── Other LLM providers (guarded imports in app/nodes) ────────────────────────
anthropic>=0.96.0

# ── Infrastructure ────────────────────────────────────────────────────────────
httpx>=0.27,<1.0
redis==5.0.4
python-dotenv>=1.0.1
aiofiles==23.2.1
docker==7.0.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24
