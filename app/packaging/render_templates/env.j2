# {{ plan.workflow_name }} — fill in real values. See .env.example for what each
# key is for and which node needs it.
#
# Values configured on the canvas are pre-filled here, including credentials.
# This file is gitignored on purpose: the committed .env.example blanks them.
{% for key in plan.env_keys %}
{{ key.key }}={{ key.default or '' }}
{% endfor %}
